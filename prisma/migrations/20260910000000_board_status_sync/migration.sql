-- 追客終了を押したときに、顧客のステータス（成約 / 失注）も合わせて変えるための列。
--
-- 戻し先を覚えておくためだけに使う。終了した人に角度を付け直して追客を
-- 再開したとき、ここに控えた元のステータスへ戻す。戻せないと、追客は
-- 動いているのに顧客一覧からは消えたまま、という食い違いが残る。
--
-- 既存のテーブルには一切触れない。追加するのは board_entries の1列のみ。
ALTER TABLE "board_entries" ADD COLUMN "statusBefore" "CustomerStatus";
